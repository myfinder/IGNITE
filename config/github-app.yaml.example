# GitHub App 設定
# このファイルをコピーして github-app.yaml として使用してください
# cp config/github-app.yaml.example config/github-app.yaml
#
# 設定手順:
# 1. GitHub App を作成: https://github.com/settings/apps/new
# 2. Private Key を生成してダウンロード
# 3. 対象リポジトリ/Organization に App をインストール
# 4. 各値を設定
#
# 詳細は docs/github-app-setup.md を参照してください

github_app:
  # GitHub App ID（必須）
  # App作成後の設定ページに表示される数字
  app_id: "YOUR_APP_ID"

  # Private Key ファイルのパス（必須）
  # 相対パスは .ignite/ ディレクトリ基準で解決されます
  # 例: "github-app-private-key.pem" → .ignite/github-app-private-key.pem
  # .pem ファイルは .gitignore で自動除外されます
  private_key_path: "github-app-private-key.pem"

  # App名（オプション）
  # コメント等でBot名を表示する際に使用
  app_name: "ignite-gh-app"
